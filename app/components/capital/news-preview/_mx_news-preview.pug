mixin news-preview()
  .news-preview
    .news-preview__container
      .news-preview__headline
        h2.s-heading-1.news-preview__heading Новости
        a.news-preview__more-link(href='news.html') Смотреть все
      ul.s-list.news-preview__list
        - for (let i = 1; i <= 2; i += 1)
          li.news-preview__item
            +new-preview()
